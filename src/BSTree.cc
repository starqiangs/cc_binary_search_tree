#include "BSTree.h"

template <typename T>
bool BSTree<T>::Insert(T data)
{
    return true;
}

template <typename T>
bool BSTree<T>::Delete(T data)
{
    return true;
}

template <typename T>
void BSTree<T>::Print()
{

}